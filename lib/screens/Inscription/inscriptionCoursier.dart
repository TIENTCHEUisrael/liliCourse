import 'package:flutter/material.dart';

class InscriptionCoursier extends StatelessWidget {
  const InscriptionCoursier({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
