import 'package:flutter/material.dart';

class InscriptionPartenaire extends StatelessWidget {
  const InscriptionPartenaire({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
